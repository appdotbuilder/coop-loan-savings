import { type LoanInstallment } from '../schema';

export async function getPendingInstallments(): Promise<LoanInstallment[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all pending/overdue installments for management view.
    // Should return unpaid installments with loan and borrower information.
    return Promise.resolve([]);
}